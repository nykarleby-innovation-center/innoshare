generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Interest {
  id       String @id @default(uuid())
  name     String
  company  String
  email    String
  language String
}
